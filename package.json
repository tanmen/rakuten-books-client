{
  "name": "rakuten-books-client",
  "version": "0.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "generate": "openapi-generator-cli generate -i openapi.yml -g typescript-axios -o src",
    "build": "tsc",
    "presemantic-release": "cp package.json README.md LICENSE dist",
    "semantic-release": "semantic-release"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.4.25",
    "@semantic-release/changelog": "^6.0.1",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^19.0.2",
    "typescript": "^4.5.5"
  }
}
